name: Archive Lint

on:
  workflow_dispatch:

jobs:
  archive-policy:
    runs-on: ubuntu-latest
    steps:
      - name: Archive policy note
        run: |
          echo "Archive trees are historical storage and excluded from CI lint/test validation."
          echo "If code is restored from archive, it must move to an active path and be validated there."
